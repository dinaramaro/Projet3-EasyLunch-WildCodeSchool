import React from 'react';
import { connect } from 'react-redux';

const ReserveSuite = () => (
  <div />

);

function mstp() {
  return {
    //ReserveSuite= state.,
  };
}

export default connect(mstp)(ReserveSuite);
